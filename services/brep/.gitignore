# Build outputs. Each /build writes a temp artifact dir; these are large,
# regenerable, and machine-specific -- never commit them.
artifacts/
__pycache__/
*.pyc
.venv/

# vision-loop run artifacts (plans, renders, model replies)
tests/vision_runs/
